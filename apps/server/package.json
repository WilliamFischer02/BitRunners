{
  "name": "@bitrunners/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bitrunners/shared": "workspace:*",
    "@bitrunners/game-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
