# Copy to .env.local for local development, or set in Cloudflare Pages env vars.

# Colyseus server URL. When unset, the client runs single-player only and never
# attempts a connection. Wire this up once apps/server is deployed to Fly.
# Example: wss://bitrunners-server.fly.dev
VITE_SERVER_URL=
