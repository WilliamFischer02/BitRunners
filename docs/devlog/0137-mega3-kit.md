# 0137 — Mega-batch 3 kit: recon-seeded prompt + launcher

## What

`mega3-prompt.md` + `setup-mega3.ps1` (repo root): the launch kit for
the owner's next local Claude Code session (Fable 5, ultracode, bypass
permissions). Same pattern as the perf-pass kit (devlog 0136); launcher
is the proven admin bootstrap with prompt/branch swapped and a
5.1-safe launch line (uses `powershell`, not `pwsh` — the script
installs PS7 itself).

## The batch (priority order)

P0 scrape-glow mask · P1 CRT soften + ASCII resolution double (cellSize
4→2, mobile-perf gated) · P2 Admin transmission face (5-frame authored
ASCII mouth cycle, typing-driven, textContent swaps — no re-renders) ·
P3 remote appearance sync (confirmed gap: outfits never on the wire +
remote rigs render no accessories; PlayerState fields + identity-
pattern message + buildRemoteAvatar rendering; Fly) · P4 interactive
scrape tutorial (TutorialHighlight reuse, economy-blob seen-flag +
merge-score entry) · P5 void netcode (zone field so void/cloud players
unload from each other + see peers; compass→door; plate press
animations/reset rules; Fly) · P6 3D asset pipeline (drop-folder
workflow, assets-registry, first GLTFLoader from three/addons) ·
P7 Data Base voxel plots (STAGED: local editor w/ RegEdit+Corporeal
tabs, depth-slider selection, 5-block palette, InstancedMesh-per-type;
palette-RLE persistence via PROPOSED migration 0019 — authored in-repo,
never applied; sky-grid hosting at y=+120 reusing the zone filter,
≤3 visitors) · P8 RAMHATTAN (gated on P6; v1 = design doc + first
slice; may spill to batch 4).

## Recon

A 3-scout workflow returned 30 file:line facts embedded in the prompt
(AdminDialogue typing state, CRT uniforms + current values, atlas
cellSize, scrape-glow DOM/CSS, the appearance-sync wire gap, void's
client-local implementation, plate detection, PlayerState/identity
message patterns, economy additive-flag pattern, protocols-registry
recipe, migration numbering + handoff pattern, empty CREDITS.md).
Line numbers are re-verified by the batch session before edits.
